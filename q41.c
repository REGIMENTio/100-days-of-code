// Write a program to swap the first and last digit of a number.

#include<stdio.h>
int main(){
    int a,b,c,d,e;
    printf("Enter number to be switched");
    scanf("%d",&a);
    if (a>10){
    b=a/10;
            }
}